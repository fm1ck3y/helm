apiVersion: v2
appVersion: "1.0"
description: A Helm chart for the etslt-devops-s3-automations application
name: my-concurency-chart
# The effective version will be computed during Helm generation
version: 0.0.1
dependencies:
  - name: helmet
    version: 0.12.0
    repository: https://charts.companyinfo.dev
    import-values: # <== It is mandatory if you want to import the Helmet default values.
      - defaults
  - name: helmize
    # Make sure to use a fixed version
    version: ">=0.0.0-0"
    repository: "https://helmize.dev/"
